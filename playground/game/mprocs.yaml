procs:
  contracts:
    cwd: packages/contracts
    shell: pnpm run dev
  client:
    cwd: packages/client
    shell: pnpm run dev
  gribi:
    cwd: packages/contracts
    shell: sleep 1 && pnpm run deploy:gribi
    autostart: true

